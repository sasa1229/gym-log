# ジムログ 設計書

## 1. 目的

ジム通いを続けている社長が、トレーニングの成果を実感し続けられるようにするための個人用PWA。「記録すること」自体が目的ではなく、**前回・過去の自分と比べて伸びているかが一目で分かること**を最優先する。

## 2. ゴール(北極星)

> 1週間、実際にジムで使い続けられるか

機能を増やす前に、まずこの状態を達成することを優先する。使われなくなる最大の原因は「入力が面倒」「情報が多すぎる」の2つ(ねむログでの反省)。

## 3. スコープ(フェーズ分け)

一度に全部作らない。1機能ずつ、実際に使ってから次に進む。

| フェーズ | 内容 | 状態 |
|---|---|---|
| v1 | ジムでのトレーニング記録(重量×回数、前回比較、自己ベスト表示) | **実装済み・v1.1でフィードバック反映済み** |
| v2 | 体重・体脂肪率の記録 | 未着手 |

v2の詳細画面設計は、v1が「1週間使えた」と確認できてから着手する。カロリー・食事記録(旧v3)は既存アプリ(あすけん)で足りているため**スコープ外**とする。

## 4. 情報設計の方針

ねむログの反省を踏まえ、以下を絶対ルールとする。

- 常時表示するのは**今日/直近の記録**と**今の数値**だけ
- 過去の全履歴・詳細な分析は、必要な時だけ別タブ・別画面で見る
- 入力は数タップで完結させる。自由入力のテキストフィールドは種目名の新規登録時のみ
- 初期セットアップ(名前入力・目標設定など)は作らない。開いてすぐ使える

## 5. 画面構成

### v1(実装済み)

| タブ | 役割 |
|---|---|
| 記録(ホーム) | 今日のトレーニングを記録する。総ボリューム/種目数/セット数を常時表示。種目ごとに前回の数値と自己ベストを表示し、更新したら炎マークが付く |
| 推移 | 種目を選んで、自己ベスト・実施回数・重量推移グラフ・過去の記録一覧を見る |

### v2(体重・体脂肪率)— たたき台

- 記録タブに「今日の体重」入力を追加(ほぼ毎日、数値ステッパー+手入力)
- 体脂肪率は体組成計を持っていないため、**米海軍式(Navy Method)**で推定する。首・腰(・身長)をメジャーで測った値から計算式で算出する。体重より測定頻度が低い前提でUIを分ける(体重=ほぼ毎日、体脂肪率=測った時だけ)
- 推移タブに体重・体脂肪率の折れ線グラフを追加

v2は着手時に改めてこの設計書に追記する。

## 10. 副業化について(現時点の方針)

将来的な副業化は視野に入れるが、今すぐ課金・広告などのマネタイズ機能は作らない。代わりに以下を意識する。

- データ構造を、後から複数ユーザー対応にしやすい形に保つ(現状のexercises/sessionsの構造なら移行コストは低い)
- 差別化の方向性:単体の筋トレ記録アプリは競合(Strong、Hevyなど)が強いが、ねむログ(睡眠)・ジムログ(トレーニング)・今後の体組成記録を「シフト勤務者の生活を丸ごと支えるアプリ群」として繋げると独自性が出せる可能性がある
- 何よりもまず、v1を実際に1週間使って価値を確認することが先

## 6. データモデル(v1)

```
State
├─ exercises: Exercise[]
│   └─ { id, name, muscle, preset }
└─ sessions: Session[]
    └─ { id, date, entries: Entry[] }
        └─ Entry: { exerciseId, sets: Set[] }
            └─ Set: { weight, reps }
```

- `date` は `YYYY-MM-DD`。1日1セッション
- 自己ベスト(PR)は保存せず、`sessions` から都度計算する(単一の真実の源を保つ)
- 保存先はブラウザの `localStorage`(サーバーなし、個人端末完結)

## 7. デザインシステム

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | `#0B0B0D` | 背景 |
| `--panel` | `#1B1C21` | カード |
| `--line` | `#2E3038` | 境界線 |
| `--ink` | `#F2F3F5` | 本文 |
| `--ink3` | `#6C6F79` | 補足文字 |
| `--accent` | `#C6FF3D` | アクセント(自己ベスト・CTA) |

- 見出し・数値:Barlow Condensed(コンデンス体、数字の並びが引き締まって見える)
- 本文:Inter
- トーン:黒×蛍光色のストイック系。装飾は最小限、数字とグラフで語る

## 8. 技術方針

- 単一HTMLファイル(`index.html`)+ PWAマニフェスト + Service Worker。ねむログと同じ構成
- フレームワーク・ビルドツールなし。ブラウザでそのまま動く
- GitHub Pagesで公開する想定(`sasa1229/gym-log`)

## 9. オープンな論点

- [x] v2の体重記録は「体重だけ」でいいか、体脂肪率も欲しいか → **両方記録する。体脂肪率は米海軍式で推定**
- [x] カロリー記録はどうするか → **スコープ外(あすけんを使う)**
- [x] 副業化は考えるか → **視野には入れるが、今は機能を作り込まない。詳細は10章**
- [ ] v1を実際にジムで1週間使ってみて、困った点・欲しい機能
